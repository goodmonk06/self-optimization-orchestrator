# Root Environment Variables
# Copy this to .env if needed for root-level configuration

# Individual services have their own .env files:
# - backend/.env.example -> backend/.env
# - dashboard/.env.example -> dashboard/.env.local
